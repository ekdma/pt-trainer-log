-- 테이블에 존재하는 최대 member_id 확인
select max(member_id) from members;

select pg_get_serial_sequence('members', 'member_id');
-- 현재 시퀀스 값 확인 (시퀀스 이름은 Supabase에서 자동 생성된 것 확인 필요)
select last_value from member_member_id_seq;

select setval('member_member_id_seq', 4, false);